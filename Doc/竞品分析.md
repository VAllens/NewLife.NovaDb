# NewLife.NovaDb 竞品分析

本文档对比 NovaDb 与主流数据库在关系型、时序、NoSQL、消息队列四大场景下的差异，用于产品定位参考。

---

## 1. 关系型数据库对比

| 特性 | SQLite | MySQL | PostgreSQL | SQL Server | **NovaDb** |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **实现语言** | C | C/C++ | C | C/C++ | **C#** |
| **开源协议** | Public Domain | GPL v2 | PostgreSQL License | 商业 | **MIT** |
| **运行形态** | 嵌入（库） | 独立服务 | 独立服务 | 独立服务 | **嵌入（库）/ 服务（进程）** |
| **部署方式** | 单文件 | 安装+配置 | 安装+配置 | 安装+配置 | **文件夹即数据库，零配置** |
| **存储结构** | B+ 树 / 单文件 | InnoDB 页/段 | 页/段 (堆表) | 页/区 | **每表独立文件组（.nvd + .nvi）+ MMF** |
| **索引结构** | B+ 树 (磁盘) | B+ 树 (磁盘) | B+ 树 / GIN / GiST | B+ 树 / 列存储 | **SkipList (内存+MMF 冷热分离)** |
| **内存管理** | 固定缓存池 | Buffer Pool (LRU) | 共享内存 (Buffer) | Buffer Pool | **动态冷热分离 (热段内存+冷段 MMF)** |
| **事务隔离** | Serializable | RC / RR / Serializable | RC / RR / Serializable | RC / SI / Serializable | **Read Committed (MVCC)** |
| **并发模型** | 单写多读 (WAL) | 多写多读 (MVCC) | 多写多读 (MVCC) | 多写多读 (锁/MVCC) | **MVCC 多读多写（嵌入跨进程额外表级文件锁）** |
| **迁移/备份** | 拷贝文件 | mysqldump / 主从 | pg_dump / 流复制 | bak 文件 / AlwaysOn | **拷贝文件夹** |
| **扩展能力** | 纯关系型 | 纯关系型 | 关系型+JSON | 关系型+JSON+图 | **关系型 + 时序 + MQ + KV** |
| **运维复杂度** | 极低 | 中 | 中 | 中偏高 | **低（单文件夹/单进程）** |
| **跨平台** | ✅ | ✅ | ✅ | Windows 为主 | **✅ (.NET 跨平台)** |
| **适用规模** | 小型/嵌入 | 中大型 | 中大型 | 中大型 | **中大型/嵌入** |
| **生态集成** | 通用 ADO | 通用 ADO | 通用 ADO | 通用 ADO | **NewLife XCode 原生集成** |

## 2. SQL92 标准实现度对比

| SQL 能力 | SQLite | MySQL | PostgreSQL | SQL Server | **NovaDb** | 备注 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| **CREATE / ALTER / DROP** | ✅ | ✅ | ✅ | ✅ | **✅** | NovaDb 支持基础 DDL |
| **INSERT / UPDATE / DELETE** | ✅ | ✅ | ✅ | ✅ | **✅** | |
| **UPSERT** | ✅ (REPLACE) | ✅ (ON DUPLICATE KEY) | ✅ (ON CONFLICT) | ✅ (MERGE) | **✅ (Upsert)** | NovaDb 原生 Upsert |
| **SELECT / WHERE / ORDER BY** | ✅ | ✅ | ✅ | ✅ | **✅** | |
| **GROUP BY / HAVING** | ✅ | ✅ | ✅ | ✅ | **✅** | |
| **聚合函数 (COUNT/SUM/AVG/MIN/MAX)** | ✅ | ✅ | ✅ | ✅ | **✅** | |
| **JOIN (INNER/LEFT/RIGHT)** | ✅ | ✅ | ✅ | ✅ | **⚠️ 基础 (Nested Loop)** | 初期不支持 Hash Join（规划支持） |
| **子查询 (IN/EXISTS)** | ✅ | ✅ | ✅ | ✅ | **⚠️ 基础** | |
| **CTE (WITH 递归)** | ✅ | ✅ (8.0+) | ✅ | ✅ | **❌** | |
| **窗口函数 (ROW_NUMBER 等)** | ✅ (3.25+) | ✅ (8.0+) | ✅ | ✅ | **❌** | |
| **视图 (VIEW)** | ✅ | ✅ | ✅ | ✅ | **❌** | |
| **外键约束** | ✅ (可选) | ✅ | ✅ | ✅ | **❌** | 不支持外键 |
| **CHECK 约束** | ✅ | ✅ (8.0.16+) | ✅ | ✅ | **❌** | |
| **触发器** | ✅ | ✅ | ✅ | ✅ | **❌** | |
| **存储过程/函数** | ❌ | ✅ | ✅ | ✅ | **❌** | |
| **事务 COMMIT/ROLLBACK** | ✅ | ✅ | ✅ | ✅ | **✅ (MVCC)** | Read Committed |
| **Serializable 隔离** | ✅ | ✅ | ✅ | ✅ | **❌** | 仅 Read Committed |
| **MERGE INTO** | ❌ | ❌ | ❌ (用 ON CONFLICT) | ✅ | **❌ (用 Upsert 替代)** | |
| **GRANT/REVOKE (权限)** | ❌ | ✅ | ✅ | ✅ | **❌ (v1 不实现)** | |
| **预估覆盖率** | ~85% | ~95% | ~98% | ~98% | **~60%** | 覆盖业务常用子集 |

> 说明：NovaDb 的 SQL 目标是"覆盖业务常用子集 + 可扩展"，不追求完整 SQL92。高级功能（CTE、窗口函数、触发器等）可在后续版本按需迭代。

## 3. 时序数据库对比

| 特性 | InfluxDB | TDengine | TimescaleDB | QuestDB | **NovaDb (Flux Engine)** |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **实现语言** | Go (v2) / Rust (v3) | C | C (PG 扩展) | Java | **C#** |
| **开源协议** | MIT (v2) / Apache 2.0 (v3) | AGPL v3 | Apache 2.0 (社区版) | Apache 2.0 | **MIT** |
| **部署方式** | 独立服务 | 独立服务/集群 | PG 扩展 | 独立服务 | **嵌入 / 独立服务** |
| **数据模型** | Tag + Field + Timestamp | 超级表 (STable) | 普通表 + 时间分区 | 表 + 时间列 | **时间分片表 (按天/小时)** |
| **写入模式** | Line Protocol (追加) | 追加写 | SQL INSERT (追加) | 追加写 | **追加写 (Append Only) + 分片** |
| **存储引擎** | TSM / IOx (列存储) | VNode 分片 | 基于 PG 堆表 + 分区 | 列存储 (零 GC) | **时间分片文件 + WAL + MMF** |
| **查询语言** | Flux / InfluxQL / SQL (v3) | 类 SQL (TQL) | 标准 SQL | 标准 SQL | **SQL (扩展语法)** |
| **降采样/聚合** | 连续查询 (Flux) | 内置 INTERVAL | TimescaleDB Toolkit | 内置采样 | **规划支持（连续查询、降采样）** |
| **数据保留 (TTL)** | ✅ (Retention Policy) | ✅ | ✅ | ✅ | **✅ (按时间/容量自动删除)** |
| **压缩** | Gorilla + RLE | 列压缩 | PG TOAST | 列压缩 | **✅（时间分片 + 冷热分离压缩）** |
| **集群/分布式** | ✅ (企业版) | ✅ (原生) | ❌ (靠 PG 方案) | ✅ (企业版) | **✅（集群 + 一主多从 Binlog 同步）** |
| **MQ 能力** | ❌ | ❌ | ❌ | ❌ | **✅ (内置 Stream/MQ + 消费组)** |
| **消息顺序保证** | ❌ | ❌ | ❌ | ❌ | **✅ (全局有序，单 Stream)** |
| **消息可靠性** | ❌ | ❌ | ❌ | ❌ | **✅ (At-Least-Once + Pending + Ack)** |
| **时间窗口聚合** | ✅ (INTERVAL 子句) | ✅ (GROUP BY <interval>) | ✅ (GROUP BY time_bucket) | ✅ | **✅ (GROUP BY INTERVAL)** |
| **运维复杂度** | 中 | 中偏高 | 低 (PG 生态) | 低 | **极低（文件夹即库）** |
| **适用规模** | 中大型 | 中大型 | 中大型 | 中大型 | **中大型/嵌入式** |
| **.NET 集成** | HTTP SDK | 原生驱动 | Npgsql | HTTP / PGWire | **原生嵌入 + ADO.NET** |

**NovaDb Flux Engine 特色说明**：
- **内置 MQ + 时序融合**：同一个引擎同时支持时序数据和消息队列，避免引入 Kafka/Redis Stream 等额外组件
- **严格顺序保证**：每个 Stream 单分区设计，保证全局消息顺序一致，消费组位点自动管理
- **消费者容灾**：支持 Pending（待确认）+ Ack（确认）机制，确保消息至少被消费一次
- **延迟队列与死信队列**：原生支持延迟投递、死信转移，无需外部配置
- **时序与 MQ 统一查询**：均支持标准 SQL 查询，包括时间窗口聚合、GROUP BY INTERVAL 等

## 4. NoSQL 数据库对比

| 特性 | Redis | MongoDB | LevelDB / RocksDB | etcd | **NovaDb (KV 模式)** |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **实现语言** | C | C++ | C++ | Go | **C#** |
| **数据模型** | KV + 多种数据结构 | 文档 (BSON) | 有序 KV | KV (强一致) | **KV 表 (Key + Value + TTL)** |
| **部署方式** | 独立服务 / 集群 | 独立服务 / 副本集 | 嵌入式库 | 独立集群 (3/5 节点) | **嵌入 / 独立服务** |
| **持久化** | RDB / AOF (可选) | WiredTiger (默认持久) | WAL + SSTable | WAL + Snapshot | **WAL + MMF (强持久化)** |
| **事务** | MULTI/EXEC (弱事务) | 多文档事务 (4.0+) | 批量写 (原子) | Mini 事务 (Revision) | **MVCC 事务 (Read Committed)** |
| **查询能力** | 命令式 (无 SQL) | 丰富查询 + 聚合管道 | Get/Put/Scan | Range + Watch | **SQL 查询 + API** |
| **内存模型** | 全内存为主 | 工作集缓存 (WiredTiger) | 块缓存 (Block Cache) | 全内存 (BoltDB) | **冷热分离 (热段内存+冷段 MMF)** |
| **数据结构** | String/Hash/List/Set/ZSet/Stream | 文档/数组/嵌套 | 有序字节 KV | 扁平 KV | **Key + Value + TTL（每行）** |
| **TTL 支持** | ✅ (Key 级别) | ✅ (TTL 索引) | ❌ (需自实现) | ✅ (Lease) | **✅（每行 TTL，支持过期清理）** |
| **发布订阅** | ✅ (Pub/Sub + Stream) | ✅ (Change Stream) | ❌ | ✅ (Watch) | **✅ (Flux Engine MQ)** |
| **分布式** | Cluster / Sentinel | 副本集 / 分片 | ❌ (嵌入式) | Raft 共识 (强一致) | **✅（集群 + 一主多从）** |
| **运维复杂度** | 低 | 中 | 极低 (嵌入) | 中 | **极低（文件夹即库）** |
| **适用规模** | 大型/超大型 | 大型/超大型 | 嵌入式 | 大型 | **大型/嵌入式** |
| **适用场景** | 缓存/会话/排行榜/队列 | 文档存储/内容管理 | 嵌入式存储/LSM 底层 | 配置中心/服务发现 | **缓存/会话/配置/分布式锁** |
| **.NET 集成** | StackExchange.Redis | MongoDB.Driver | 需 P/Invoke | dotnet-etcd | **原生嵌入 + ADO.NET** |

**NovaDb KV 模式特色说明**：
- **多表隔离**：同一数据库支持多个独立的 KV 表，每表可配置不同默认 TTL
- **SQL + API 双访问**：既支持标准 SQL 查询，也提供 Get/Set/Add/Inc/Remove 等编程 API
- **冷热分离**：KV 数据也支持冷热分离，常访问的热键常驻内存，冷键自动转移到 MMF
- **TTL 与过期清理**：每行支持 TTL 设置，过期数据自动不可见，后台异步清理

## 5. 消息队列对比

| 特性 | Kafka | RabbitMQ | Redis Stream | RocketMQ | Pulsar | **NovaDb (Flux MQ)** |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| **实现语言** | Java/Scala | Erlang | C | Java | Java | **C#** |
| **部署复杂度** | 高 (ZK/KRaft + Broker) | 中 (Erlang 运行时) | 低 (Redis 实例) | 中 (NameServer + Broker) | 高 (ZK + BK + Broker) | **极低（嵌入或单进程）** |
| **消息模型** | 分区 + 消费组 (Pull) | Exchange + Queue (Push/Pull) | Stream + 消费组 (Pull) | Topic + Queue (Pull) | Topic + 订阅 (Pull/Push) | **Stream + 消费组 (Pull)** |
| **消息持久化** | ✅ 磁盘 (顺序写) | ✅ 磁盘 (可选) | ✅ (AOF/RDB，可选) | ✅ 磁盘 (CommitLog) | ✅ 磁盘 (BookKeeper) | **✅ WAL + 时间分片文件** |
| **消息可靠性** | At-Least-Once / Exactly-Once | At-Least-Once / At-Most-Once | At-Least-Once | At-Least-Once | At-Least-Once / Exactly-Once | **At-Least-Once** |
| **消息顺序** | 分区内有序 | 单队列有序 | 全局有序 | 分区内有序 | 分区内有序 | **全局有序 (单 Stream)** |
| **消息回溯** | ✅ (按 Offset) | ❌ | ✅ (按 ID/时间) | ✅ (按时间) | ✅ (按 ID/时间) | **✅ (按 ID/时间)** |
| **延迟消息** | ❌ (需扩展) | ✅ (TTL + DLX) | ❌ | ✅ (内置) | ✅ (内置) | **✅ (DelayTime/DeliverAt)** |
| **死信队列** | ❌ (需自建) | ✅ (DLX) | ❌ | ✅ (内置) | ✅ (内置) | **✅ (DLQ + 重试机制)** |
| **阻塞读取** | ✅ (长轮询) | ✅ (Push 模式) | ✅ (XREADGROUP BLOCK) | ✅ (长轮询) | ✅ (Push/长轮询) | **✅ (阻塞 + 超时)** |
| **消息 TTL/保留** | ✅ (按时间/大小) | ✅ (Per-Message TTL) | ✅ (MAXLEN/MINID) | ✅ (按时间) | ✅ (按时间/大小) | **✅ (按时间/大小自动删除)** |
| **吞吐量** | 极高 (百万/s) | 中 (万级/s) | 高 (十万/s) | 高 (十万/s) | 极高 (百万/s) | **中等（受 .NET GC 与单机限制）** |
| **SQL 查询消息** | ❌ (KSQL 独立组件) | ❌ | ❌ | ❌ | ✅ (Pulsar SQL) | **✅ (原生 SQL 查询)** |
| **运维可观测** | Kafka Manager / JMX | Management UI | INFO / MONITOR | Dashboard | Admin API | **系统表 SQL 查询** |
| **集群** | ✅ (原生) | ✅ (镜像队列/仲裁队列) | ✅ (Redis Cluster) | ✅ (原生) | ✅ (原生) | **✅（集群 + 主从同步）** |
| **适用规模** | 大型/超大型 | 大型/超大型 | 中大型 | 大型/超大型 | 大型/超大型 | **大型/嵌入式** |
| **.NET 集成** | Confluent.Kafka | RabbitMQ.Client | StackExchange.Redis | rocketmq-client-csharp | DotPulsar | **原生嵌入 + ADO.NET** |
| **适用场景** | 大数据管道/事件溯源 | 企业集成/任务分发 | 轻量级内部队列 | 电商/金融 | 多租户/云原生 | **嵌入式内部队列 / IoT 消息** |

## 6. 向量数据库对比

| 特性 | Pinecone | Weaviate | Milvus | Qdrant | **NovaDb (Vector 支持)** |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **实现语言** | Proprietary (托管) | Go | C++ | Rust | **C#** |
| **开源协议** | 专有/SaaS | BSL + 社区版 | Apache 2.0 | AGPL/商用 | **MIT** |
| **部署方式** | 云端 (SaaS) | 独立服务 | 独立服务/集群 | 独立服务/集群 | **嵌入 / 独立服务** |
| **向量维度支持** | 高达 20000 | 无限制 | 32768 | 65535 | **1~4096（可配置）** |
| **索引算法** | 专有 (PIXT) | HNSW + LSH | IVF / HNSW / SCANN | HNSW | **HNSW / IVF（规划）** |
| **数据模型** | 向量 + 元数据 | 向量 + 对象 (JSON) | 向量 + 标量数据 | 向量 + payload | **向量 + 关系型表（融合）** |
| **查询语言** | REST API / SDK | GraphQL + REST | SQL 子集 | REST API | **SQL（原生支持）** |
| **混合查询** | ❌ | ✅ (向量 + 向量搜索) | ⚠️（部分） | ✅ (向量 + 标量过滤) | **✅ (完全集成，向量 + 关系型联合查询)** |
| **KNN 查询性能** | 极高 (云优化) | 高 | 高 | 高 | **中等（单机限制）** |
| **数据过滤** | ✅ (元数据过滤) | ✅ | ✅ (WHERE 子句) | ✅ (Payload 过滤) | **✅ (完整 SQL WHERE)** |
| **相似度算法** | 点积 / 余弦 / 欧氏距离 | 余弦 / 欧氏距离 | 多种 | 点积 / 余弦 / 欧氏距离 | **点积 / 余弦 / 欧氏距离** |
| **批量插入** | ✅ | ✅ | ✅ | ✅ | **✅** |
| **更新/删除** | ✅ | ✅ | ✅ | ✅ | **✅ (标准 SQL UPDATE/DELETE)** |
| **事务支持** | ❌ | ❌ | ❌ | ❌ | **✅ (MVCC 事务)** |
| **集群/分布式** | ✅ (SaaS) | ✅ | ✅ | ✅ | **✅（集群 + 主从）** |
| **运维复杂度** | 极低 (SaaS) | 中 | 中偏高 | 中 | **极低（文件夹即库）** |
| **适用规模** | 大型 (SaaS) | 大型 | 大型/超大型 | 大型 | **大型/嵌入式** |
| **.NET 集成** | REST SDK | REST / GraphQL | Milvus.io SDK | REST API | **原生嵌入 + ADO.NET** |

**NovaDb Vector 融合特色说明**：
- **向量与关系数据融合**：同一个 SQL 查询中可以既执行向量相似搜索，也执行关系型过滤，避免应用层两次查询
- **完整 SQL 支持**：不仅支持 KNN 查询，还支持向量上的 WHERE/GROUP BY/ORDER BY 等完整 SQL 操作
- **MVCC 事务一致性**：向量数据与关系数据在同一事务框架内，保证一致性
- **成本更低**：不需要单独部署向量数据库，减少运维复杂度和集成成本

## 7. 地理数据库与 LBS 对比

| 特性 | PostGIS (PG 扩展) | MongoDB Geospatial | Elasticsearch Geo | H3/S2 库 | **NovaDb (GeoPoint 支持)** |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **实现语言** | C (PG 扩展) | C++ | Java | 各语言实现 | **C#** |
| **数据类型** | Point / LineString / Polygon 等 | Point / LineString / Polygon | GeoPoint / Shape | H3 Index / S2 Cell | **GeoPoint（经纬度）** |
| **索引支持** | GiST / BRIN / GIST | 2dsphere / 2d 索引 | GEO 索引 | 格网索引 | **GeoHash / R-Tree（规划）** |
| **查询能力** | ST_Distance / ST_Contains 等 | $near / $geoWithin | geo_distance / geo_polygon | grid_cell / ring | **DISTANCE / WITHIN_RADIUS** |
| **距离计算** | 多种 (Haversine/Great Circle) | Haversine | Haversine | 格网距离 | **Haversine（大圆距离）** |
| **范围查询** | ✅ (ST_Within / ST_Intersects) | ✅ ($geoWithin) | ✅ (geo_bounding_box) | ✅ (ring_contains) | **✅ (WITHIN_RADIUS / WITHIN_POLYGON 规划)** |
| **最近邻查询** | ✅ (ORDER BY distance) | ✅ ($near) | ✅ (sort by geo_distance) | ❌ | **✅ (Top-K 规划)** |
| **热数据自动缓存** | ❌ | ❌ | ❌ | ❌ | **✅ (冷热分离)** |
| **集成关系查询** | ✅ (标准 SQL) | ⚠️（部分） | ✅ | ❌ | **✅ (完整 SQL)** |
| **运维复杂度** | 低 (PG 生态) | 中 | 中 | 低 (库级) | **极低（文件夹即库）** |
| **适用场景** | 专业 GIS / 地图服务 | 内部 LBS 功能 | 日志分析 + 地理维度 | 大规模热力图/网格统计 | **嵌入式 LBS 功能** |
| **.NET 集成** | Npgsql + NetTopologySuite | MongoDB.Driver | NEST | S2Geometry | **原生嵌入 + ADO.NET** |

**NovaDb GeoPoint 融合特色说明**：
- **地理查询与关系型联合**：同一 SQL 可以既执行地理范围搜索，也执行业务条件过滤
- **无需外部 GIS 库**：内置 GeoPoint 类型与地理函数，不需要 PostGIS 复杂配置
- **冷热分离优化**：大规模坐标数据自动分离，热点位置常驻内存，冷数据按需加载
- **轻量级部署**：不依赖 PostGIS，作为 .NET 应用同进程运行，零额外部署开销

## 8. NovaDb 综合差异化总结

| 差异化能力 | 说明 |
| :--- | :--- |
| **纯 .NET 托管** | 无 Native 依赖，支持 .NET Framework 4.5+，部署即 xcopy，与 .NET 应用同进程零序列化开销 |
| **嵌入+服务双模** | 开发调试嵌入如 SQLite，生产部署独立服务如 MySQL，同一套 API |
| **集群与主从同步** | 独立服务支持集群部署，支持一主多从同步，用于读扩展与容灾；多主多从作为远期目标 |
| **文件夹即数据库** | 拷贝文件夹完成迁移/备份，无需 dump/restore 流程 |
| **冷热分离索引** | 1000 万行表仅查热点时内存 < 20MB，冷数据自动卸载至 MMF |
| **关系型 + 时序 + MQ + KV + 向量 + 地理融合** | 单一引擎覆盖 SQLite + TDengine + Redis + Milvus + PostGIS 常见场景，减少组件数与集成复杂度 |
| **向量与关系数据无缝融合** | 同一 SQL 查询中支持向量 KNN 搜索与关系型过滤，避免应用层两次查询，提升开发效率 |
| **地理位置与业务数据统一** | 原生支持 GeoPoint 类型与地理函数（距离/范围/最近邻），无需 PostGIS，轻量级部署 |
| **NewLife XCode 原生集成** | ORM 层直接适配，无需第三方驱动 |

## 9. 使用场景定位

### 何时选择 NovaDb 而非竞品

| 使用场景 | NovaDb 适配度 | 推荐原因 | 不推荐的竞品 |
| :--- | :--- | :--- | :--- |
| **嵌入式 .NET 应用** | ⭐⭐⭐⭐⭐ | 无部署复杂度，与 C# 代码同进程，零序列化开销 | SQLite (性能+容量不足) |
| **小型 .NET SaaS 应用** | ⭐⭐⭐⭐⭐ | 文件夹即数据库，快速迁移，运维极简 | MySQL/PostgreSQL (学习+运维成本) |
| **物联网/IoT 数据采集** | ⭐⭐⭐⭐ | 内置时序 + MQ，支持 100 亿行/日消息，消息顺序保证 | TDengine (学习成本) / Kafka (过重) |
| **位置服务 (LBS) 功能** | ⭐⭐⭐⭐ | 原生 GeoPoint + 地理函数，与业务数据统一，无外部依赖 | PostGIS (部署成本) |
| **向量语义搜索** | ⭐⭐⭐⭐ | 向量与关系型数据融合，同一 SQL 联合查询，降低架构复杂度 | Milvus (部署/集成成本) |
| **内部任务队列** | ⭐⭐⭐⭐ | 消息顺序保证，Pending + Ack 可靠投递，延迟队列 + DLQ 原生支持 | Redis Stream (单机) / RabbitMQ (过重) |
| **会话/缓存存储** | ⭐⭐⭐⭐ | KV 模式 + TTL，支持 API 与 SQL 双访问，冷热分离降低内存 | Redis (全内存，成本高) |
| **配置中心/密钥存储** | ⭐⭐⭐⭐ | KV 模式，本地文件持久化，嵌入式部署，无网络开销 | etcd (集群) |
| **日志/指标存储** | ⭐⭐⭐ | 内置时序，支持按天分片，自动 TTL 清理 | TDengine (功能更全) |
| **大规模 OLTP 系统** | ⭐⭐ | 单机限制性能，不适合超大规模在线交易 | MySQL/PostgreSQL/Oracle |
| **复杂报表/OLAP 分析** | ⭐ | 列存储能力有限，不适合复杂多维分析 | ClickHouse / Presto |

---
