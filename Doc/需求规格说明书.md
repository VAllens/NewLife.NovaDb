# NewLife.NovaDb 需求规格说明书

## 1. 产品概述
*   **全称**：NewLife.NovaDb（诺娃数据库）
*   **简称**：Nova（诺娃）
*   **定位**：以 **C#** 实现的中大型混合数据库，支持嵌入式/服务器双模，融合关系型、时序、消息队列、NoSQL(KV) 能力。
*   **运行平台**：引擎与客户端驱动（ADO.NET Provider）均支持 **.NET Framework 4.5+**（含 .NET Core / .NET 5~10），纯托管代码，运行于 .NET 支持的所有操作系统（Windows/Linux/macOS）与指令集（x86/x64/ARM/ARM64）。
*   **愿景**：NewLife 生态核心基础设施，替代 SQLite/MySQL/Redis/TDengine 在特定场景的使用，一套引擎覆盖关系型、时序、MQ、KV 四大场景。
*   **约束**：纯托管代码（C#/.NET），不依赖 Native 组件。v1 不做平台特定优化；后续版本可针对向量运算等场景利用 SIMD 硬件加速。
*   **目标**：通过裁剪大量冷门能力（如存储过程/触发器/窗口函数等），换取更高的读写性能与更低的运维成本；数据量逻辑上无上限（受磁盘与切分策略约束）。

## 2. 核心架构

| 模块 | 规格说明 | 设计意图 |
| :--- | :--- | :--- |
| **部署模式** | **嵌入模式** (本地文件夹) / **服务器模式** (独立进程 TCP/IPv6，支持一主多从集群) | 嵌入如 SQLite，网络如 MySQL |
| **文件管理** | **文件夹即数据库**，每表独立文件组 (`.nvd`, `.nvi`, `.wal`)，无需分片 | 拷贝文件夹即完成迁移/备份 |
| **存储介质** | **MMF (内存映射文件)** 为主，物理内存为辅 | 64 位系统虚拟地址空间 128TB，突破物理内存限制，支持超大表 |
| **索引策略** | **动态冷热分离** (见 3.1 节) | 热点快，冷点省内存 |
| **并发模型** | 引擎内部统一 **MVCC 多读多写**（行级不阻塞），嵌入模式额外增加**表级文件锁**（跨进程单写协调）；两种模式共享同一套引擎代码 | 每表独立文件组让锁粒度缩小到单表，远优于 SQLite 库级锁 |
| **WAL 日志** | **FULL** (同步) / **NORMAL** (异步 1s) / **NONE** (全异步)，表级独立 WAL | 写入时产生 `.wal`，checkpoint 后删除；只读模式不创建 `.wal` |
| **只读模式** | 嵌入模式支持 **ReadOnly** 连接，禁止所有写操作 (DDL/DML) | 跳过 WAL 初始化、关闭脏页刷盘，提升读取性能；规避多进程并发写冲突；适用于报表查询、数据分析、从库只读副本等场景 |
| **通信协议** | 自定义二进制协议 (TCP/IPv6)，较大数据包自动启用压缩 | 极致性能，大包自动压缩减少网络传输，ADO.NET 驱动自动识别 |

## 3. 存储引擎详解

### 3.1 默认引擎：Nova Engine (通用关系型)
*   **数据结构**：SkipList (跳表)。
*   **为何选择跳表**：

    | 维度 | SkipList (跳表) | B+ Tree | LSM Tree |
    | :--- | :--- | :--- | :--- |
    | **写入性能** | 高（O(log N)，无需再平衡） | 中（O(log N)，可能触发页分裂/合并） | 极高（顺序追加 + 后台 Compaction） |
    | **点查询** | 高（O(log N)） | 高（O(log N)，缓存友好） | 中（可能查多层 SSTable） |
    | **范围扫描** | 中（链表结构，缓存局部性稍弱） | 高（叶子链表顺序扫描） | 中（需合并多层结果） |
    | **并发友好** | **极高**（天然支持无锁读、细粒度写） | 中（页分裂需加锁协调） | 高（写入无争用，Compaction 需协调） |
    | **托管实现** | **简单**（纯 C# 实现简洁，无复杂分裂/合并逻辑） | 复杂（页分裂/合并/再平衡代码量大） | 复杂（多层 Compaction 策略复杂） |
    | **内存开销** | 中偏高（每节点多层指针） | 低（紧凑页面布局） | 低（磁盘为主，内存仅缓存） |
    | **空间放大** | 无 | 无 | 高（多层副本 + 删除延迟回收） |

    **结论**：跳表在并发友好性和托管实现简单性上优势明显，配合 MMF 冷热分离可弥补内存开销劣势；范围扫描性能稍弱于 B+ 树，但对业务 CRUD 场景影响可接受。
*   **容量目标**：单表支持 **无限行**（上限取决于磁盘空间，64 位系统下 MMF 虚拟地址空间 128TB，无需分片）。
*   **内存优化 (核心)**：
    *   **热数据 (Hot)**：最近访问 (如最近 10 分钟) 的数据段，索引完整加载至物理内存 (SkipList 节点)。
    *   **冷数据 (Cold)**：长期未访问数据，索引卸载至 MMF，内存仅保留**稀疏目录** (如每 1 万行一个锚点)。
    *   **效果**：1000 万行表，若只查最新 1 万行，内存占用 < 20MB；若全表随机查，依赖 MMF 页 fault，性能下降但可用。
    *   **淘汰机制**：LRU 策略，超过阈值 (如 30 分钟未访问) 自动卸载热索引。
*   **持久化 (核心)**：
    *   **单表单文件**：每张表一个 `.nvd` 数据文件，逻辑上不设行数上限，数据增长主要消耗磁盘资源。10G、100G 的数据文件在 MMF 下没有问题，OS 只映射实际访问的页，物理内存按需分配。
    *   **不分片**：Nova Engine 不做自动数据分片。如需逻辑分表（按时间/Hash），由 XCode ORM 层在应用层面完成。
    *   **默认文件布局**：每表独立文件组（`.nvd` + 多个 `.nvi` + 写入时的 `.wal`）。
    *   **冷热加载原则**：表数据极少读取或不读取时，不应占用显著内存；只有频繁读取的热点范围，才提升为内存热索引。
*   **索引文件 (核心)**：
    *   **多索引多文件**：一张表每个索引（主键/二级索引/唯一索引等）对应独立索引文件（主键索引为 `{TableName}.nvi`，二级索引为 `{TableName}_{IndexName}.nvi`），便于独立加载、淘汰与重建。
    *   **nvi 不是内存跳表节点镜像**：`.nvi` 存储可持久化的“目录 + 块/页”结构（稀疏目录锚点 -> 块偏移），用于按需 MMF 映射与快速定位；内存 SkipList 用作热缓存结构，可从 `.nvi` 按需重建热点区域。
    *   **文件组命名**：每表文件组使用相同表名前缀，通过后缀区分（`.nvd`/`.wal`/`.nvi`），所有文件平铺在数据库目录下。
*   **并发控制**：
    *   **引擎内部（统一）**：MVCC 多读多写，读不阻塞写，写不阻塞读。写操作创建新行版本，读操作看到一致快照。嵌入模式和服务器模式**共享同一套引擎代码**。
    *   **嵌入模式（跨进程）**：在 MVCC 之上增加表级文件锁（`{TableName}.lock`），协调多个进程对同一张表的写入——同一时刻只有一个进程可写入某张表，多个进程可同时读取。不同表的读写互不影响。进程内部仍是 MVCC 多读多写。
    *   **服务器模式**：单进程运行，无需文件锁，MVCC 直接提供行级多读多写。
    *   **相比 SQLite**：SQLite 是库级锁（写入时锁整个数据库文件），NovaDb 每表独立文件组让锁粒度缩小到单表，写 `User` 表不影响 `App` 表的读写。
*   **适用场景**：通用 CRUD，配置表，业务订单，用户数据。

### 3.2 时序引擎：Flux Engine (时序 + MQ)
*   **数据结构**：按时间分片 (如每天/每小时一个 `.nvd` 文件)，追加写 (Append Only)。
*   **容量目标**：单表支持 **无限行** (依赖磁盘空间)。时间分片自然限制单个文件大小。
*   **统一数据模型**：时序数据与 MQ 消息采用同一存储结构。**每一条 MQ 消息就是时序表的一行数据**（MessageId + Fields），Stream/Topic 名即时序表名。发送消息 = 向时序表追加一行，消费消息 = 按 MessageId 顺序读取行。
*   **MQ 功能 (Stream)**：
    *   **模型**：参考 Redis Stream 的消费组模型（`Topic/Stream` + `ConsumerGroup` + `Consumer` + `Pending`）。
    *   **消息 ID**：时间戳 + 序列（同毫秒内自增），整体单调递增。消息 ID 同时也是时序表的主键。
    *   **可靠性**：At-Least-Once，读取后进入 Pending，业务成功后 Ack。
    *   **阻塞读取**：支持阻塞 + 超时（类似 `XREADGROUP BLOCK`）。
    *   **延迟消息**：
        *   **实现机制**：消息发送时指定 `DelayTime`（延迟时长，秒）或 `DeliverAt`（投递时间），消息写入延迟队列（专用索引）。
        *   **投递策略**：后台扫描线程按时间顺序轮询延迟队列，到期消息自动转入正常队列供消费。
        *   **应用场景**：订单超时自动取消、定时通知、重试退避策略。
    *   **死信队列 (Dead Letter Queue, DLQ)**：
        *   **触发条件**：消息消费失败达到最大重试次数（可配置，默认 3 次）或消费超时后，自动进入死信队列。
        *   **存储结构**：每个普通队列对应一个独立死信队列（`{QueueName}_DLQ`），保留原始消息 + 失败原因 + 失败次数。
        *   **处理机制**：
            *   死信队列可独立订阅，用于人工介入或异常告警。
            *   支持死信消息重新投递回原队列（Retry）或永久删除。
        *   **应用场景**：消息处理异常排查、消息丢失防护、业务降级处理。
    *   **可观测性**：系统表记录队列、消费组、消费者、Pending、延迟消息、死信队列等状态，便于 SQL 查询使用情况。
*   **数据保留**：支持 TTL (按时间/文件大小自动删除旧分片)。
*   **适用场景**：物联网传感器数据，日志收集，内部消息队列，审计日志，定时任务调度。

### 3.3 KV 模式 (逻辑视图)
*   **实现**：
    *   **引擎复用**：不单独建引擎，复用 Nova Engine，通过 API 屏蔽 SQL 细节。
    *   **Schema**：固定结构的 Nova 表 (`Key` STRING(200) PK, `Value` BLOB, `TTL` DATETIME)。
    *   **多表支持**：一个数据库可以创建多个 KV 表，通过 `ENGINE=KV` 区分。不同 KV 表互相独立，可用于不同业务场景（如 `SessionCache`、`ConfigCache`、`DistLock`），每个表可配置不同的默认 TTL。
*   **创建方式**：
    *   **SQL**：`CREATE TABLE SessionCache (Key STRING(200) PRIMARY KEY, Value BLOB, TTL DATETIME) ENGINE=KV`
    *   **API**：`db.CreateKvStore("SessionCache", defaultTtl: TimeSpan.FromHours(2))`
*   **TTL 机制**：
    *   **强制过期**：所有 key **必须**设置过期时间（`TTL`，绝对 UTC 时间表示到期），防止无限期驻留内存。
    *   **自动清理**：后台线程定期扫描并删除过期记录（`NOW() > TTL`），保持内存与磁盘高效。
    *   **默认策略**：未显式指定时使用 KV 表级默认 TTL（如 24 小时），可在建表时配置。
*   **接口**：
    *   **SQL**：`SELECT Value FROM SessionCache WHERE Key = '...' AND TTL > NOW()`
    *   **API**：
        *   `Get(key)`：获取值（自动忽略过期）。
        *   `Set(key, value, ttl)`：设置/更新值及过期时间。
        *   `Add(key, value, ttl)`：仅当 key 不存在时添加（分布式锁场景）。
        *   `Remove(key)`：删除 key。
        *   `Inc(key, delta, ttl)`：原子递增（计数器场景，key 不存在时初始化为 delta）。
*   **适用场景**：分布式锁，缓存，会话存储，计数器，配置中心。

### 3.4 存储系统设计

#### 3.4.1 文件夹即数据库
每个数据库对应一个目录，目录下平铺所有文件。拷贝目录即完成数据库迁移/备份。

#### 3.4.2 数据库元数据文件 (`nova.db`)
二进制格式，程序可读即可，不需要人工读取。包含整个数据库的完整元数据：
*   **FileHeader** (32 字节)：魔数 `NOVA` (0x4E4F5641)、版本号 (1B)、文件类型 FileType=Metadata (1B)、页大小位移 (1B)、特性标志 (1B)、创建时间 UTC 毫秒 (8B)、CRC32 校验和 (4B)、预留 (12B)。
*   **数据库信息**：数据库名称、创建时间、最后修改时间。
*   **表目录**：所有表的定义，每张表包含：
    *   表名、引擎类型（Nova/Flux/KV）、注释、创建时间。
    *   列定义列表：列名、数据类型、长度、是否可空、是否主键、是否自增、默认值、注释。
    *   索引定义列表：索引名、是否唯一、包含的列名列表。
*   **修改机制**：CREATE/ALTER/DROP TABLE 时原子重写 `nova.db`（先写临时文件再原子重命名），保证崩溃安全。
*   **启动加载**：引擎启动时一次性读取 `nova.db`，在内存中构建表 Schema 缓存。

#### 3.4.3 数据库文件组织

所有文件平铺在数据库目录下，分为**数据库级文件**和**表级文件**两类。

**数据库级文件**：

| 文件 | 命名规则 | 说明 |
| :--- | :--- | :--- |
| 元数据文件 | `nova.db` | 数据库完整元数据（表/列/索引定义），二进制格式 |
| Binlog 日志 | `nova.{SeqNo:D6}.binlog` | 主从同步/增量备份的逻辑日志，数据库级（详见 3.4.5 节） |

**表级文件组**（每张表由以下文件组成）：

| 文件 | 后缀 | 说明 |
| :--- | :--- | :--- |
| 数据文件 | `.nvd` | 表的行数据，每表一个，通过 MMF 访问 |
| 主键索引 | `.nvi` | 主键索引文件，每表一个 |
| 二级索引 | `_{IndexName}.nvi` | 每个二级索引独立一个文件 |
| WAL 日志 | `.wal` | 写入时产生，checkpoint 后删除 |
| 写锁文件 | `.lock` | 嵌入模式下的表级写锁（服务器模式不需要） |

表级文件命名规则：
*   数据文件：`{TableName}.nvd`
*   主键索引：`{TableName}.nvi`
*   二级索引：`{TableName}_{IndexName}.nvi`
*   WAL 文件：`{TableName}.wal`
*   写锁文件：`{TableName}.lock`
*   时序分片（仅 Flux Engine）：`{TableName}_{yyyyMMdd}.nvd` 或 `{TableName}_{yyyyMMddHH}.nvd`

#### 3.4.4 WAL 生命周期
*   **写入阶段**：事务写入时，WAL 记录追加到 `{TableName}.wal`。
*   **Checkpoint**：后台线程将 WAL 中的脏页刷新到 `.nvd` 文件。
*   **清理**：Checkpoint 完成后，`.wal` 文件删除（或截断为 0）。
*   **崩溃恢复**：启动时发现 `.wal` 文件存在 → 执行 WalRecovery 重放已提交事务 → 恢复后删除 `.wal`。
*   **只读模式**：不创建 `.wal` 文件，跳过 WAL 初始化。

#### 3.4.5 Binlog 日志（主从同步/增量备份）

Binlog 是数据库级别的逻辑日志，记录所有已提交事务的逻辑操作（INSERT/UPDATE/DELETE/DDL），用于主从复制、增量备份和审计追踪。

**WAL 与 Binlog 的区别**：

| 维度 | WAL | Binlog |
| :--- | :--- | :--- |
| **层级** | 表级（每表一个 `.wal`） | 数据库级（全库共享 `.binlog`） |
| **记录内容** | 物理页面变更（UpdatePage） | 逻辑操作（INSERT/UPDATE/DELETE/DDL） |
| **用途** | 崩溃恢复 | 主从复制 + 增量备份 + 审计 |
| **生命周期** | 短暂（checkpoint 后删除） | 持久保留（按策略滚动清理） |
| **写入时机** | 事务写入时（提交前） | 事务提交后 |
| **可读性** | 二进制页面数据 | 结构化逻辑记录（可解析重放） |

##### 3.4.5.1 使用场景

| 场景 | 说明 |
| :--- | :--- |
| **主从复制** | 主节点写入 Binlog，从节点拉取并重放，实现数据同步 |
| **增量备份** | 备份工具记录上次备份的 LSN，下次仅拉取后续 Binlog 重放 |
| **审计追踪** | Binlog 记录了谁在什么时间对哪张表做了什么操作 |
| **数据恢复** | 全量备份 + Binlog 重放 = 恢复到任意时间点（Point-in-Time Recovery） |

##### 3.4.5.2 启用方式

| 模式 | 默认状态 | 配置方式 |
| :--- | :--- | :--- |
| **嵌入模式** | 不启用 | `DbOptions.EnableBinlog = true`，或连接字符串 `EnableBinlog=true` |
| **服务器模式** | 启用 | 默认启用，可通过配置关闭 |

##### 3.4.5.3 文件管理

**文件命名**：`nova.{SeqNo:D6}.binlog`（如 `nova.000001.binlog`、`nova.000002.binlog`）。

**滚动机制**：
*   当前活跃的 Binlog 文件接受顺序追加写入。
*   单个文件达到大小阈值（默认 256MB，通过 `BinlogMaxFileSize` 配置）时，关闭当前文件并创建下一个序号文件。
*   **序号永远递增**，不会回绕或重用。
*   每个文件的 FileHeader 记录该文件的起始 LSN 和创建时间，便于快速定位。

**文件内索引**：Binlog 文件开头的 FileHeader 之后紧跟一段索引区域，记录文件内 LSN 范围（起始 LSN ~ 结束 LSN）。引擎通过索引区域可快速判断某个 LSN 落在哪个文件中，无需逐文件扫描。

##### 3.4.5.4 自动清理策略

引擎后台线程定期检查并清理过期 Binlog，支持三种策略（可叠加使用，任一条件满足即清理）：

| 策略 | 配置项 | 默认值 | 说明 |
| :--- | :--- | :--- | :--- |
| **从节点确认** | 自动 | - | 所有从节点已确认同步的文件可安全清理（主从模式的首要依据） |
| **按天数保留** | `BinlogRetentionDays` | 0（不限） | 超过保留天数的旧文件自动删除 |
| **按总大小限制** | `BinlogMaxTotalSize` | 0（不限） | 所有 Binlog 文件总大小超过上限时，从最早的文件开始删除 |

**清理安全性**：
*   被清理的文件必须是**已关闭的旧文件**，当前正在写入的活跃文件永远不会被清理。
*   按天数或总大小清理时，**不检查从节点同步状态**——这是运维人员的主动决策，引擎尊重。如果从节点还未同步完被删除的文件，该从节点将需要全量重建。

##### 3.4.5.5 运维操作（手工管理）

NovaDb 的 Binlog 文件设计为**可安全手工删除**的独立文件。每个 `.binlog` 文件自包含（FileHeader + 完整的 Binlog 记录序列），文件之间无交叉依赖。

**直接删除旧文件**：
*   运维人员可直接在文件系统上删除旧的 `.binlog` 文件释放磁盘空间，**引擎不会崩溃或异常**。
*   引擎启动时扫描数据库目录，按文件名排序重建 Binlog 文件列表，自动感知哪些文件已被删除。
*   **唯一代价**：如果从节点请求的 LSN 落在已删除的文件中，该从节点将收到"Binlog 已清理"错误，需要通过全量复制（拷贝数据库目录）重新同步。

**与 MySQL 运维体验对比**：

| 操作 | MySQL | NovaDb |
| :--- | :--- | :--- |
| 查看 Binlog 列表 | `SHOW BINARY LOGS` | `SHOW BINLOGS` |
| 查看当前活跃 Binlog | `SHOW MASTER STATUS` | `SHOW BINLOG STATUS` |
| 手工删除指定序号之前的文件 | `PURGE BINARY LOGS TO 'binlog.000005'` | `PURGE BINLOGS TO 'nova.000005.binlog'` |
| 按时间删除 | `PURGE BINARY LOGS BEFORE '2025-07-01'` | `PURGE BINLOGS BEFORE '2025-07-01'` |
| 直接 rm 文件 | 官方不建议，可能导致索引文件不一致 | **安全**，引擎自动感知 |
| 关闭当前文件并滚动到新文件 | `FLUSH BINARY LOGS` | `FLUSH BINLOGS` |

**SQL 管理命令详细说明**：
*   `SHOW BINLOGS`：列出所有 Binlog 文件（文件名、大小、起始 LSN、结束 LSN、创建时间）。
*   `SHOW BINLOG STATUS`：显示当前活跃 Binlog 文件名、当前 LSN、文件大小。
*   `PURGE BINLOGS TO 'nova.000005.binlog'`：删除序号 5 之前的所有文件（保留 5 及之后的文件）。
*   `PURGE BINLOGS BEFORE '2025-07-01 00:00:00'`：删除创建时间早于指定时间的所有文件。
*   `FLUSH BINLOGS`：立即关闭当前活跃文件并滚动到新文件（用于备份前确保文件完整）。

##### 3.4.5.6 主从复制流程

1.  主节点事务提交 → 写入 WAL（崩溃恢复） + Binlog（复制同步）。
2.  主节点将 Binlog 记录流式推送给从节点。
3.  从节点接收 Binlog 记录，重放逻辑操作，更新本地数据。
4.  从节点回报已同步的 LSN，主节点可据此清理已确认的 Binlog。

**断点续传**：从节点宕机恢复后，携带最后已同步的 LSN 向主节点请求后续 Binlog，无需全量复制。

**增量备份**：备份工具记录上次备份的 LSN，下次备份时仅拉取后续 Binlog 重放即可，远快于全量拷贝。

**全量重建**：当从节点请求的 LSN 对应的 Binlog 已被清理时，需要通过全量复制（拷贝整个数据库目录）重新同步，之后从最新 LSN 开始增量同步。

##### 3.4.5.7 配置选项

| 配置项 | 默认值 | 说明 |
| :--- | :--- | :--- |
| `EnableBinlog` | false (嵌入) / true (服务器) | 是否启用 Binlog |
| `BinlogMaxFileSize` | 256MB | 单文件滚动阈值 |
| `BinlogRetentionDays` | 0 | 保留天数，0 表示不按时间清理 |
| `BinlogMaxTotalSize` | 0 | 总大小上限（字节），0 表示不限制 |
#### 3.4.6 并发与锁

| 模式 | 引擎并发 | 跨进程锁 | 说明 |
| :--- | :--- | :--- | :--- |
| **嵌入模式** | MVCC 多读多写（进程内） | 表级文件锁 (`{TableName}.lock`)，跨进程单写多读 | 进程内 MVCC 提供行级不阻塞；跨进程通过文件锁协调写入，不同表互不影响 |
| **服务器模式** | MVCC 多读多写 | 无（单进程） | 所有请求通过 SqlEngine 统一调度，MVCC 保证行级读写不阻塞 |
| **只读模式** | 只读 | 无锁 | 不创建 `.wal` 和 `.lock`，多进程可同时以只读方式打开 |

**代码统一**：嵌入模式与服务器模式的引擎代码完全相同，均使用 MVCC 实现多读多写。表级文件锁仅是嵌入模式跨进程的附加协调层。

**与 SQLite 对比**：SQLite 使用库级锁，写入时锁定整个数据库文件，所有表读写都受影响。NovaDb 每表独立文件组让锁粒度缩小到单表，写 `User` 表不影响 `App` 表的读写，并发度显著提升。

#### 3.4.7 数据库文件示例

以星尘 (StarDust) 数据库为例，包含用户表、应用表、应用日志（时序+MQ）、会话缓存（KV）：

```
StarDust/                              ← 文件夹即数据库
├── nova.db                            ← 数据库元数据（二进制：所有表/列/索引定义）
│
│   ── Binlog 日志（主从同步/增量备份）──
├── nova.000001.binlog                  ← 已滚动的 Binlog（从节点确认后可清理）
├── nova.000002.binlog                  ← 当前活跃的 Binlog（正在追加写入）
│
│   ── Nova Engine 关系表 ──
├── User.nvd                           ← 用户表数据（可能 500MB~10GB）
├── User.nvi                           ← 用户表主键索引（Id）
├── User_Name.nvi                      ← 用户表 Name 二级索引
├── User_Email.nvi                     ← 用户表 Email 唯一索引
├── User.wal                           ← 用户表 WAL（写入中存在，checkpoint 后消失）
├── User.lock                          ← 用户表写锁（嵌入模式，写入时存在）
│
├── App.nvd                            ← 应用表数据
├── App.nvi                            ← 应用表主键索引
├── App_Name.nvi                       ← 应用表 Name 索引
│
│   ── Flux Engine 时序表（时序 + MQ）──
├── AppLog.nvd                         ← 应用日志当前分片
├── AppLog.nvi                         ← 日志表时间主键索引
├── AppLog_20250101.nvd                ← 日志按天历史分片
├── AppLog_20250102.nvd                ← 日志按天历史分片
├── AppLog_20250103.nvd                ← 日志按天历史分片
│
├── AppMeter.nvd                       ← 应用性能指标时序表
├── AppMeter.nvi                       ← 指标表时间索引
│
├── TaskQueue.nvd                      ← 任务队列（Flux Engine MQ 模式）
├── TaskQueue.nvi                      ← 队列时间索引
│
│   ── KV Engine 键值表 ──
├── SessionCache.nvd                   ← 会话缓存 KV 表
├── SessionCache.nvi                   ← KV 表主键索引（Key）
│
├── ConfigCache.nvd                    ← 配置缓存 KV 表
└── ConfigCache.nvi                    ← KV 表主键索引（Key）
```

**文件内容结构概要**：

| 文件类型 | 内部结构 |
| :--- | :--- |
| `nova.db` | `FileHeader(32B)` + 数据库信息 + 表目录（表名/引擎/列定义/索引定义）序列化 |
| `{Table}.nvd` | `FileHeader(32B)` + 数据页序列（每页 = `PageHeader(32B)` + 行数据），MMF 按需映射 |
| `{Table}.nvi` | `FileHeader(32B)` + 索引页序列（稀疏目录锚点 → 数据页偏移），支持冷热分离 |
| `{Table}.wal` | WAL 记录流（`BeginTx → UpdatePage → CommitTx`），顺序追加，checkpoint 后删除 |
| `nova.*.binlog` | Binlog 记录流（`LSN + TableName + OpType + RowData`），顺序追加，按滚动策略清理 |
| `{Table}.lock` | 0 字节文件，仅用 `FileStream` 排他锁实现互斥 |

## 4. 数据类型与函数

### 4.1 数据类型

| 类别 | 类型定义 | C# 映射 | 说明 |
| :--- | :--- | :--- | :--- |
| **布尔** | `BOOL` | `Boolean` | 真/假，1 字节 |
| **整数** | `INT` | `Int32` | 32 位整数，4 字节 |
| | `LONG` / `BIGINT` | `Int64` | 64 位整数，8 字节 |
| **浮点** | `DOUBLE` | `Double` | 双精度浮点，8 字节 |
| **定点** | `DECIMAL` | `Decimal` | 128 位定点数，**统一精度**，无需声明 `(p,s)` |
| **字符串** | `STRING(n)` | `String` | UTF-8 编码，**必须指定长度** `n`（字节数），用于索引优化与存储分配 |
| | `STRING` | `String` | 无长度限制（动态），**不建议用于索引列** |
| **二进制** | `BINARY(n)` / `VARBINARY(n)` | `Byte[]` | **必须指定长度** `n`（字节数） |
| | `BLOB` | `Byte[]` | 大对象，无长度限制（动态） |
| **时间** | `DATETIME` | `DateTime` | 精确到 Ticks（100 纳秒），支持时序排序 |
| **地理编码** | `GEOPOINT` | 自定义结构 | 经纬度坐标 `(Lat, Lon)`，16 字节（双 `Double`） |
| **向量** | `VECTOR(n)` | `Single[]` | 定长浮点数组，用于 AI 向量检索，如 `VECTOR(1536)` |

#### 4.1.1 类型设计原则
*   **精度统一**：`DECIMAL` 固定 128 位，开发者无需关心精度/标度，引擎内部保证精度足够业务使用。
*   **长度必需**：`STRING(n)` / `BINARY(n)` 必须指定长度，便于引擎优化索引（定长索引性能更高）与页布局。
*   **动态类型**：`STRING` / `BLOB` 无长度限制，但**不适合作为索引列**（索引需截断或哈希）。
*   **NULL 约束**：所有类型默认允许 `NULL`，支持 `NOT NULL` 约束（`CREATE TABLE` 时声明）。

#### 4.1.2 地理编码类型 (GeoPoint)
*   **存储**：`struct { double Lat; double Lon; }`，共 16 字节。
*   **计算函数**：
    *   `DISTANCE(point1, point2)`：计算两点距离（米），Haversine 公式。
    *   `WITHIN_RADIUS(point, center, radius)`：判断点是否在圆形范围内。
*   **索引**：基于 GeoHash 或 R-Tree 的空间索引，支持范围查询（BoundingBox）。
*   **适用场景**：LBS 应用，附近商家/用户查询，地理围栏。

#### 4.1.3 向量类型 (Vector)
*   **存储**：定长 `float[]` 数组，如 `VECTOR(1536)` 占用 1536 × 4 = 6144 字节。
*   **计算函数**：
    *   `COSINE_SIMILARITY(vec1, vec2)`：余弦相似度（-1 到 1）。
    *   `EUCLIDEAN_DISTANCE(vec1, vec2)`：欧氏距离。
    *   `DOT_PRODUCT(vec1, vec2)`：点积。
*   **索引**：基于 HNSW (层次可导航小世界图) 或 IVF (倒排文件) 的向量索引，支持 KNN (K 近邻) 查询。
*   **适用场景**：AI 向量检索（语义搜索、推荐系统、RAG 应用），支持 OpenAI Embeddings、文本向量化等。

### 4.2 SQL 函数支持清单

以下按功能分类列出 SQL92 及扩展函数，标注实现优先级：
*   **P0（必须）**：v1 版本必须实现，覆盖 80% 业务场景。
*   **P1（高）**：v1 建议实现，覆盖 95% 业务场景。
*   **P2（可选）**：v2+ 按需实现，非核心功能。
*   **不支持**：明确不实现（复杂度高/场景少）。

---

#### 4.2.1 聚合函数

| 函数 | 优先级 | 说明 |
| :--- | :--- | :--- |
| `COUNT(*)` / `COUNT(column)` | **P0** | 行计数，支持 `DISTINCT` |
| `SUM(column)` | **P0** | 数值求和 |
| `AVG(column)` | **P0** | 平均值 |
| `MIN(column)` / `MAX(column)` | **P0** | 最小/最大值 |
| `STRING_AGG(column, sep)` | **P1** | 字符串聚合（拼接） |
| `GROUP_CONCAT(column)` | **P1** | MySQL 风格字符串聚合 |
| `STDDEV(column)` / `VARIANCE(column)` | **P2** | 标准差/方差（统计场景） |

---

#### 4.2.2 字符串函数

| 函数 | 优先级 | 说明 |
| :--- | :--- | :--- |
| `CONCAT(str1, str2, ...)` | **P0** | 字符串拼接 |
| `LENGTH(str)` / `LEN(str)` | **P0** | 字符串长度（字节数） |
| `SUBSTRING(str, pos, len)` | **P0** | 子串提取（`pos` 从 1 开始） |
| `UPPER(str)` / `LOWER(str)` | **P0** | 大小写转换 |
| `TRIM(str)` / `LTRIM(str)` / `RTRIM(str)` | **P0** | 去除空白 |
| `REPLACE(str, from, to)` | **P0** | 字符串替换 |
| `LEFT(str, n)` / `RIGHT(str, n)` | **P0** | 左/右截取 |
| `CHARINDEX(substr, str)` / `INSTR(substr, str)` | **P1** | 查找子串位置（返回索引，不存在返回 0） |
| `REVERSE(str)` | **P2** | 反转字符串 |
| `LPAD(str, len, pad)` / `RPAD(str, len, pad)` | **P2** | 左/右填充至指定长度 |
| `FORMAT(value, format)` | **不支持** | 复杂格式化（应用层实现） |
| `SOUNDEX(str)` / `DIFFERENCE(str1, str2)` | **不支持** | 音似匹配（冷门） |

---

#### 4.2.3 数值函数

| 函数 | 优先级 | 说明 |
| :--- | :--- | :--- |
| `ABS(n)` | **P0** | 绝对值 |
| `ROUND(n, decimals)` | **P0** | 四舍五入（`decimals` 为 0 时取整） |
| `CEILING(n)` / `FLOOR(n)` | **P0** | 向上/向下取整 |
| `POWER(base, exp)` / `SQRT(n)` | **P1** | 幂运算/平方根 |
| `MOD(n, m)` / `%` | **P0** | 取模 |
| `RAND()` / `RANDOM()` | **P1** | 随机数（0-1） |
| `SIGN(n)` | **P1** | 符号（-1/0/1） |
| `TRUNCATE(n, decimals)` | **P1** | 截断（不四舍五入） |
| `PI()` | **P2** | 圆周率常数 |
| `EXP(n)` / `LOG(n)` / `LOG10(n)` | **P2** | 指数/自然对数/常用对数 |
| `SIN(n)` / `COS(n)` / `TAN(n)` | **不支持** | 三角函数（冷门） |
| `ASIN(n)` / `ACOS(n)` / `ATAN(n)` | **不支持** | 反三角函数 |

---

#### 4.2.4 日期时间函数

| 函数 | 优先级 | 说明 |
| :--- | :--- | :--- |
| `NOW()` / `GETDATE()` / `CURRENT_TIMESTAMP` | **P0** | 当前时间 |
| `YEAR(date)` / `MONTH(date)` / `DAY(date)` | **P0** | 提取年/月/日 |
| `HOUR(time)` / `MINUTE(time)` / `SECOND(time)` | **P0** | 提取时/分/秒 |
| `DATEDIFF(date1, date2)` | **P0** | 日期差（天数） |
| `DATEADD(interval, n, date)` / `DATE_ADD(date, INTERVAL n unit)` | **P0** | 日期加减（`unit`: YEAR/MONTH/DAY/HOUR 等） |
| `DATEPART(part, date)` | **P1** | 提取日期部分（通用版本，`part`: YEAR/MONTH/DAY 等） |
| `DATE_FORMAT(date, format)` | **P1** | 格式化输出（如 `'%Y-%m-%d'`） |
| `WEEKDAY(date)` / `DAYOFWEEK(date)` | **P1** | 星期几（1-7） |
| `LAST_DAY(date)` | **P2** | 月末日期 |
| `TIMESTAMPDIFF(unit, date1, date2)` | **P2** | 时间戳差（按单位） |
| `TIMESTAMPADD(unit, n, date)` | **P2** | 时间戳加减 |

---

#### 4.2.5 类型转换函数

| 函数 | 优先级 | 说明 |
| :--- | :--- | :--- |
| `CAST(value AS type)` | **P0** | 标准类型转换（如 `CAST(123 AS STRING)`） |
| `CONVERT(type, value)` | **P0** | SQL Server 风格转换 |
| `COALESCE(v1, v2, ...)` | **P0** | 返回第一个非 NULL 值 |
| `ISNULL(value, default)` / `IFNULL(value, default)` | **P0** | NULL 替换 |
| `NULLIF(v1, v2)` | **P1** | 相等时返回 NULL，否则返回 v1 |

---

#### 4.2.6 条件函数

| 函数 | 优先级 | 说明 |
| :--- | :--- | :--- |
| `CASE WHEN ... THEN ... ELSE ... END` | **P0** | 条件表达式（标准 SQL） |
| `IF(condition, true_val, false_val)` | **P0** | MySQL 风格条件（三元运算符） |
| `IIF(condition, true_val, false_val)` | **P1** | SQL Server 风格条件 |

---

#### 4.2.7 系统/元数据函数

| 函数 | 优先级 | 说明 |
| :--- | :--- | :--- |
| `DATABASE()` / `CURRENT_DATABASE()` | **P1** | 当前数据库名 |
| `VERSION()` | **P1** | 数据库版本（如 `NovaDb 1.0.2024.1201`） |
| `ROW_COUNT()` / `@@ROWCOUNT` | **P1** | 上一条 DML 影响的行数 |
| `LAST_INSERT_ID()` / `@@IDENTITY` | **P1** | 最后插入的自增主键 ID |
| `USER()` / `CURRENT_USER()` | **P2** | 当前用户（v1 无权限系统，返回默认值 `'nova'`） |
| `CONNECTION_ID()` | **P2** | 当前连接 ID |

---

#### 4.2.8 地理/向量函数（NovaDb 扩展）

| 函数 | 优先级 | 说明 |
| :--- | :--- | :--- |
| `DISTANCE(point1, point2)` | **P1** | 地理距离（米），Haversine 公式 |
| `WITHIN_RADIUS(point, center, radius)` | **P1** | 判断点是否在圆形范围内 |
| `COSINE_SIMILARITY(vec1, vec2)` | **P1** | 向量余弦相似度 |
| `EUCLIDEAN_DISTANCE(vec1, vec2)` | **P1** | 向量欧氏距离 |
| `DOT_PRODUCT(vec1, vec2)` | **P2** | 向量点积 |

---

#### 4.2.9 加密/哈希函数

| 函数 | 优先级 | 说明 |
| :--- | :--- | :--- |
| `MD5(str)` | **P1** | MD5 哈希（32 字符十六进制） |
| `SHA1(str)` | **P1** | SHA1 哈希（40 字符十六进制） |
| `SHA2(str, bits)` | **P1** | SHA2 哈希（`bits`: 256/384/512） |
| `ENCRYPT(str, key)` / `AES_ENCRYPT(str, key)` | **不支持** | 加密（应用层实现） |

---

#### 4.2.10 正则表达式函数

| 函数 | 优先级 | 说明 |
| :--- | :--- | :--- |
| `REGEXP(str, pattern)` / `RLIKE(str, pattern)` | **不支持** | 正则匹配（性能开销大） |
| `REGEXP_REPLACE(str, pattern, replacement)` | **不支持** | 正则替换 |

---

### 4.3 约束支持

| 约束类型 | 支持度 | 说明 |
| :--- | :--- | :--- |
| **主键 (PRIMARY KEY)** | ✅ | 唯一且非 NULL，自动创建索引 |
| **索引 (INDEX)** | ✅ | 普通索引、唯一索引（`UNIQUE INDEX`） |
| **NOT NULL** | ✅ | 列级约束，`CREATE TABLE` 时声明 |
| **默认值 (DEFAULT)** | ✅ | 列级约束，如 `DEFAULT 0`、`DEFAULT NOW()` |
| **外键 (FOREIGN KEY)** | ❌ | 不支持（简化设计） |
| **CHECK 约束** | ❌ | 不支持（v1 不实现） |
| **自增 (AUTO_INCREMENT)** | ✅ | 主键自增，仅支持 `INT`/`LONG` 类型 |

## 5. SQL 与功能支持

| 功能 | 支持度 | 说明 |
| :--- | :--- | :--- |
| **DDL** | ✅ 高 | CREATE/DROP DATABASE, CREATE/DROP TABLE, ALTER TABLE (ADD/MODIFY/DROP COLUMN, COMMENT), ENGINE |
| **DML** | ✅ 高 | Insert/Update/Delete/Upsert/Truncate |
| **查询** | ✅ 中 | Select/Where/Order/Group By |
| **连接** | ⚠️ 基础 | **Join** (Nested Loop)，未来支持 **Hash Join**；**Subquery** (IN/Exists) |
| **事务** | ✅ 高 | MVCC, Read Committed, Commit/Rollback |
| **MQ 订阅** | ✅ 新增 | `CREATE CONSUMER`, `COMMIT OFFSET` (扩展语法/API) |
| **NoSQL** | ✅ 新增 | KV 表专用 API 支持（每行 TTL：`Key + Value + TTL`），支持分布式部署 |
| **Binlog 管理** | ✅ 新增 | `SHOW BINLOGS`, `SHOW BINLOG STATUS`, `PURGE BINLOGS TO/BEFORE`, `FLUSH BINLOGS` |
| **高级** | ❌ 无 | 无视图/触发器/存储过程 |

## 6. 主流数据库对比分析

### 6.1 关系型数据库对比

| 特性 | SQLite | MySQL | PostgreSQL | SQL Server | **NovaDb** |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **实现语言** | C | C/C++ | C | C/C++ | **C#** |
| **开源协议** | Public Domain | GPL v2 | PostgreSQL License | 商业 | **MIT** |
| **运行形态** | 嵌入（库） | 独立服务 | 独立服务 | 独立服务 | **嵌入（库）/ 服务（进程）** |
| **部署方式** | 单文件 | 安装+配置 | 安装+配置 | 安装+配置 | **文件夹即数据库，零配置** |
| **存储结构** | B+ 树 / 单文件 | InnoDB 页/段 | 页/段 (堆表) | 页/区 | **每表独立文件组（.nvd + .nvi）+ MMF** |
| **索引结构** | B+ 树 (磁盘) | B+ 树 (磁盘) | B+ 树 / GIN / GiST | B+ 树 / 列存储 | **SkipList (内存+MMF 冷热分离)** |
| **内存管理** | 固定缓存池 | Buffer Pool (LRU) | 共享内存 (Buffer) | Buffer Pool | **动态冷热分离 (热段内存+冷段 MMF)** |
| **事务隔离** | Serializable | RC / RR / Serializable | RC / RR / Serializable | RC / SI / Serializable | **Read Committed (MVCC)** |
| **并发模型** | 单写多读 (WAL) | 多写多读 (MVCC) | 多写多读 (MVCC) | 多写多读 (锁/MVCC) | **MVCC 多读多写（嵌入跨进程额外表级文件锁）** |
| **迁移/备份** | 拷贝文件 | mysqldump / 主从 | pg_dump / 流复制 | bak 文件 / AlwaysOn | **拷贝文件夹** |
| **扩展能力** | 纯关系型 | 纯关系型 | 关系型+JSON | 关系型+JSON+图 | **关系型 + 时序 + MQ + KV** |
| **运维复杂度** | 极低 | 中 | 中 | 中偏高 | **低（单文件夹/单进程）** |
| **跨平台** | ✅ | ✅ | ✅ | Windows 为主 | **✅ (.NET 跨平台)** |
| **适用规模** | 小型/嵌入 | 中大型 | 中大型 | 中大型 | **中大型/嵌入** |
| **生态集成** | 通用 ADO | 通用 ADO | 通用 ADO | 通用 ADO | **NewLife XCode 原生集成** |

### 6.2 SQL92 标准实现度对比

| SQL 能力 | SQLite | MySQL | PostgreSQL | SQL Server | **NovaDb** | 备注 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| **CREATE / ALTER / DROP** | ✅ | ✅ | ✅ | ✅ | **✅** | NovaDb 支持基础 DDL |
| **INSERT / UPDATE / DELETE** | ✅ | ✅ | ✅ | ✅ | **✅** | |
| **UPSERT** | ✅ (REPLACE) | ✅ (ON DUPLICATE KEY) | ✅ (ON CONFLICT) | ✅ (MERGE) | **✅ (Upsert)** | NovaDb 原生 Upsert |
| **SELECT / WHERE / ORDER BY** | ✅ | ✅ | ✅ | ✅ | **✅** | |
| **GROUP BY / HAVING** | ✅ | ✅ | ✅ | ✅ | **✅** | |
| **聚合函数 (COUNT/SUM/AVG/MIN/MAX)** | ✅ | ✅ | ✅ | ✅ | **✅** | |
| **JOIN (INNER/LEFT/RIGHT)** | ✅ | ✅ | ✅ | ✅ | **⚠️ 基础 (Nested Loop)** | 初期不支持 Hash Join（规划支持） |
| **子查询 (IN/EXISTS)** | ✅ | ✅ | ✅ | ✅ | **⚠️ 基础** | |
| **CTE (WITH 递归)** | ✅ | ✅ (8.0+) | ✅ | ✅ | **❌** | |
| **窗口函数 (ROW_NUMBER 等)** | ✅ (3.25+) | ✅ (8.0+) | ✅ | ✅ | **❌** | |
| **视图 (VIEW)** | ✅ | ✅ | ✅ | ✅ | **❌** | |
| **外键约束** | ✅ (可选) | ✅ | ✅ | ✅ | **❌** | 不支持外键 |
| **CHECK 约束** | ✅ | ✅ (8.0.16+) | ✅ | ✅ | **❌** | |
| **触发器** | ✅ | ✅ | ✅ | ✅ | **❌** | |
| **存储过程/函数** | ❌ | ✅ | ✅ | ✅ | **❌** | |
| **事务 COMMIT/ROLLBACK** | ✅ | ✅ | ✅ | ✅ | **✅ (MVCC)** | Read Committed |
| **Serializable 隔离** | ✅ | ✅ | ✅ | ✅ | **❌** | 仅 Read Committed |
| **MERGE INTO** | ❌ | ❌ | ❌ (用 ON CONFLICT) | ✅ | **❌ (用 Upsert 替代)** | |
| **GRANT/REVOKE (权限)** | ❌ | ✅ | ✅ | ✅ | **❌ (v1 不实现)** | |
| **预估覆盖率** | ~85% | ~95% | ~98% | ~98% | **~60%** | 覆盖业务常用子集 |

> 说明：NovaDb 的 SQL 目标是"覆盖业务常用子集 + 可扩展"，不追求完整 SQL92。高级功能（CTE、窗口函数、触发器等）可在后续版本按需迭代。

### 6.3 时序数据库对比

| 特性 | InfluxDB | TDengine | TimescaleDB | QuestDB | **NovaDb (Flux Engine)** |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **实现语言** | Go (v2) / Rust (v3) | C | C (PG 扩展) | Java | **C#** |
| **开源协议** | MIT (v2) / Apache 2.0 (v3) | AGPL v3 | Apache 2.0 (社区版) | Apache 2.0 | **MIT** |
| **部署方式** | 独立服务 | 独立服务/集群 | PG 扩展 | 独立服务 | **嵌入 / 独立服务** |
| **数据模型** | Tag + Field + Timestamp | 超级表 (STable) | 普通表 + 时间分区 | 表 + 时间列 | **时间分片表 (按天/小时)** |
| **写入模式** | Line Protocol (追加) | 追加写 | SQL INSERT (追加) | 追加写 | **追加写 (Append Only)** |
| **存储引擎** | TSM / IOx (列存储) | VNode 分片 | 基于 PG 堆表 + 分区 | 列存储 (零 GC) | **时间分片文件 + WAL + MMF** |
| **查询语言** | Flux / InfluxQL / SQL (v3) | 类 SQL (TQL) | 标准 SQL | 标准 SQL | **SQL (扩展语法)** |
| **降采样/聚合** | 连续查询 (Flux) | 内置 INTERVAL | TimescaleDB Toolkit | 内置采样 | **计划支持（优先级较低）** |
| **数据保留 (TTL)** | ✅ (Retention Policy) | ✅ | ✅ | ✅ | **✅ (按时间/大小自动删除)** |
| **压缩** | Gorilla + RLE | 列压缩 | PG TOAST | 列压缩 | **✅（冷数据分片压缩）** |
| **集群/分布式** | ✅ (企业版) | ✅ (原生) | ❌ (靠 PG 方案) | ✅ (企业版) | **✅（集群 + 主从同步）** |
| **MQ 能力** | ❌ | ❌ | ❌ | ❌ | **✅ (内置 Stream/MQ)** |
| **运维复杂度** | 中 | 中偏高 | 低 (PG 生态) | 低 | **极低（文件夹即库）** |
| **适用规模** | 中大型 | 中大型 | 中大型 | 中大型 | **中大型/嵌入** |
| **.NET 集成** | HTTP SDK | 原生驱动 | Npgsql | HTTP / PGWire | **原生嵌入 + ADO.NET** |

### 6.4 NoSQL 数据库对比

| 特性 | Redis | MongoDB | LevelDB / RocksDB | etcd | **NovaDb (KV 模式)** |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **实现语言** | C | C++ | C++ | Go | **C#** |
| **数据模型** | KV + 多种数据结构 | 文档 (BSON) | 有序 KV | KV (强一致) | **KV 表 (Key + Value + TTL)** |
| **部署方式** | 独立服务 / 集群 | 独立服务 / 副本集 | 嵌入式库 | 独立集群 (3/5 节点) | **嵌入 / 独立服务** |
| **持久化** | RDB / AOF (可选) | WiredTiger (默认持久) | WAL + SSTable | WAL + Snapshot | **WAL + MMF (强持久化)** |
| **事务** | MULTI/EXEC (弱事务) | 多文档事务 (4.0+) | 批量写 (原子) | Mini 事务 (Revision) | **MVCC 事务 (Read Committed)** |
| **查询能力** | 命令式 (无 SQL) | 丰富查询 + 聚合管道 | Get/Put/Scan | Range + Watch | **SQL 查询 + API** |
| **内存模型** | 全内存为主 | 工作集缓存 (WiredTiger) | 块缓存 (Block Cache) | 全内存 (BoltDB) | **冷热分离 (热段内存+冷段 MMF)** |
| **数据结构** | String/Hash/List/Set/ZSet/Stream | 文档/数组/嵌套 | 有序字节 KV | 扁平 KV | **Key + Value + TTL（每行）** |
| **TTL 支持** | ✅ (Key 级别) | ✅ (TTL 索引) | ❌ (需自实现) | ✅ (Lease) | **✅（每行 TTL）** |
| **发布订阅** | ✅ (Pub/Sub + Stream) | ✅ (Change Stream) | ❌ | ✅ (Watch) | **✅ (Flux Engine MQ)** |
| **分布式** | Cluster / Sentinel | 副本集 / 分片 | ❌ (嵌入式) | Raft 共识 (强一致) | **✅（集群）** |
| **运维复杂度** | 低 | 中 | 极低 (嵌入) | 中 | **极低（文件夹即库）** |
| **适用规模** | 大型/超大型 | 大型/超大型 | 嵌入式 | 大型 | **大型/嵌入式** |
| **适用场景** | 缓存/会话/排行榜/队列 | 文档存储/内容管理 | 嵌入式存储/LSM 底层 | 配置中心/服务发现 | **缓存/会话/配置/分布式锁** |
| **.NET 集成** | StackExchange.Redis | MongoDB.Driver | 需 P/Invoke | dotnet-etcd | **原生嵌入 + ADO.NET** |

### 6.5 消息队列对比

| 特性 | Kafka | RabbitMQ | Redis Stream | RocketMQ | Pulsar | **NovaDb (Flux MQ)** |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| **实现语言** | Java/Scala | Erlang | C | Java | Java | **C#** |
| **部署复杂度** | 高 (ZK/KRaft + Broker) | 中 (Erlang 运行时) | 低 (Redis 实例) | 中 (NameServer + Broker) | 高 (ZK + BK + Broker) | **极低（嵌入或单进程）** |
| **消息模型** | 分区 + 消费组 (Pull) | Exchange + Queue (Push/Pull) | Stream + 消费组 (Pull) | Topic + Queue (Pull) | Topic + 订阅 (Pull/Push) | **Stream + 消费组 (Pull)** |
| **消息持久化** | ✅ 磁盘 (顺序写) | ✅ 磁盘 (可选) | ✅ (AOF/RDB，可选) | ✅ 磁盘 (CommitLog) | ✅ 磁盘 (BookKeeper) | **✅ WAL + 时间分片文件** |
| **消息可靠性** | At-Least-Once / Exactly-Once | At-Least-Once / At-Most-Once | At-Least-Once | At-Least-Once | At-Least-Once / Exactly-Once | **At-Least-Once** |
| **消息顺序** | 分区内有序 | 单队列有序 | 全局有序 | 分区内有序 | 分区内有序 | **全局有序 (单 Stream)** |
| **消息回溯** | ✅ (按 Offset) | ❌ | ✅ (按 ID/时间) | ✅ (按时间) | ✅ (按 ID/时间) | **✅ (按 ID/时间)** |
| **延迟消息** | ❌ (需扩展) | ✅ (TTL + DLX) | ❌ | ✅ (内置) | ✅ (内置) | **✅ (DelayTime/DeliverAt)** |
| **死信队列** | ❌ (需自建) | ✅ (DLX) | ❌ | ✅ (内置) | ✅ (内置) | **✅ (DLQ + 重试机制)** |
| **阻塞读取** | ✅ (长轮询) | ✅ (Push 模式) | ✅ (XREADGROUP BLOCK) | ✅ (长轮询) | ✅ (Push/长轮询) | **✅ (阻塞 + 超时)** |
| **消息 TTL/保留** | ✅ (按时间/大小) | ✅ (Per-Message TTL) | ✅ (MAXLEN/MINID) | ✅ (按时间) | ✅ (按时间/大小) | **✅ (按时间/大小自动删除)** |
| **吞吐量** | 极高 (百万/s) | 中 (万级/s) | 高 (十万/s) | 高 (十万/s) | 极高 (百万/s) | **中等（受 .NET GC 与单机限制）** |
| **SQL 查询消息** | ❌ (KSQL 独立组件) | ❌ | ❌ | ❌ | ✅ (Pulsar SQL) | **✅ (原生 SQL 查询)** |
| **运维可观测** | Kafka Manager / JMX | Management UI | INFO / MONITOR | Dashboard | Admin API | **系统表 SQL 查询** |
| **集群** | ✅ (原生) | ✅ (镜像队列/仲裁队列) | ✅ (Redis Cluster) | ✅ (原生) | ✅ (原生) | **✅（集群 + 主从同步）** |
| **适用规模** | 大型/超大型 | 大型/超大型 | 中大型 | 大型/超大型 | 大型/超大型 | **大型/嵌入式** |
| **.NET 集成** | Confluent.Kafka | RabbitMQ.Client | StackExchange.Redis | rocketmq-client-csharp | DotPulsar | **原生嵌入 + ADO.NET** |
| **适用场景** | 大数据管道/事件溯源 | 企业集成/任务分发 | 轻量级内部队列 | 电商/金融 | 多租户/云原生 | **嵌入式内部队列 / IoT 消息** |

### 6.6 NovaDb 综合差异化总结

| 差异化能力 | 说明 |
| :--- | :--- |
| **纯 .NET 托管** | 无 Native 依赖，支持 .NET Framework 4.5+，部署即 xcopy，与 .NET 应用同进程零序列化开销 |
| **嵌入+服务双模** | 开发调试嵌入如 SQLite，生产部署独立服务如 MySQL，同一套 API |
| **集群与主从同步** | 独立服务支持集群部署，支持一主多从同步，用于读扩展与容灾；多主多从作为远期目标 |
| **文件夹即数据库** | 拷贝文件夹完成迁移/备份，无需 dump/restore 流程 |
| **冷热分离索引** | 1000 万行表仅查热点时内存 < 20MB，冷数据自动卸载至 MMF |
| **关系型 + 时序 + MQ + KV 融合** | 单一引擎覆盖 SQLite + TDengine + Redis 常见场景，减少组件数 |
| **NewLife XCode 原生集成** | ORM 层直接适配，无需第三方驱动 |

## 7. 关键设计决策汇总

1.  **索引冷热分离**：
    *   **机制**：内存维护“热段 SkipList" + “冷段稀疏目录”。
    *   **触发**：基于访问时间 (LRU)，后台线程定期卸载冷索引。
    *   **目标**：1000 万行表，仅查最新 1 万行时，内存占用 < 20MB。
2.  **超大表支持（不分片）**：
    *   **上限**：逻辑上无限制（受磁盘空间约束）。
    *   **MMF 保障**：64 位系统虚拟地址空间 128TB，10G/100G 数据文件 OS 按需分页映射，物理内存仅消耗实际访问的页。
    *   **不分片**：Nova Engine 不做自动数据分片，简化引擎设计。如需分表由 XCode 应用层完成。
    *   **代价**：冷数据查询性能随数据量下降（依赖 MMF 页 fault），可能出现秒级响应。
    *   **建议**：XCode 层仍建议对超大数据进行逻辑分表 (按时间/Hash)。
3.  **表级并发控制**：
    *   **引擎统一**：嵌入模式和服务器模式共享同一套 MVCC 引擎代码，引擎内部始终是多读多写、行级不阻塞。
    *   **嵌入模式（跨进程）**：在 MVCC 之上增加表级文件锁（`{TableName}.lock`），协调跨进程写入——同一时刻只有一个进程可写入某张表，不同表互不影响，远优于 SQLite 库级锁。
    *   **服务器模式**：单进程运行，无需文件锁，MVCC 直接提供行级多读多写。
    *   **星尘场景**：StarWeb 和 StarServer 同时访问 NovaDb 数据库时，写进程持表锁，读进程用 `FileShare.Read` 并发读取。写 `User` 表不影响读 `App` 表。
4.  **MQ 可靠性**：
    *   **模型**：At-Least-Once (至少一次)。
    *   **统一存储**：MQ 消息就是时序表的一行数据（MessageId + Fields），Stream/Topic 即时序表名。
    *   **流程**：读取消息 -> 进入 Pending -> 业务处理 -> Ack/确认（服务端更新消费组偏移量并移除 Pending）。
    *   **风险**：崩溃重试可能导致重复消费，需业务层幂等。
    *   **参考**：整体模型参考 Redis Stream 的消费组语义（服务端维护每个消费组的消费进度与 Pending）。
5.  **WAL 生命周期**：
    *   **写入时产生**：事务写入时追加到 `{TableName}.wal`。
    *   **用完即删**：Checkpoint 将脏页刷到 `.nvd` 后，`.wal` 文件删除。
    *   **崩溃恢复**：启动时发现 `.wal` 存在则自动重放恢复。
    *   **只读模式**：不创建 `.wal` 文件。
6.  **nova.db 元数据**：
    *   **二进制格式**：不需要人工可读，程序可读即可。
    *   **内容**：数据库信息 + 完整的表/列/索引定义。
    *   **原子更新**：DDL 操作时先写临时文件再原子重命名，保证崩溃安全。
7.  **KV 多表**：
    *   **允许多个 KV 表**：通过 `ENGINE=KV` 创建，不同 KV 表用于不同业务场景。
    *   **独立 TTL**：每个 KV 表可配置不同的默认 TTL。

---
